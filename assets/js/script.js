// Variables
var todoColor = $('.todo-color');
var rowEl = $('.rows');

// Curent Date and Time in the header
var currentDay = moment().format('dddd, MMMM Do, h:mm a');
$('#currentDay').text(currentDay);

// Assigns light-grey color to the To Do column if the time has passed current time


// Assigns green color to the TO Do column if the time has not passed.


//Assigns red color to the TO Do column of the current hour. 

//